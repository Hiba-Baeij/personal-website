/** @type {import('next').NextConfig} */
// import { NextConfig } from "next";
// export default {
//   reactStrictMode: true,

// } as NextConfig

const nextConfig = {
  experimental: {
    // appDir: true,
    reactStrictMode: true,
  },
};

export default nextConfig;
